
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create User</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-950 text-white min-h-screen">

    <!-- NAVBAR -->
    <nav class="w-full bg-zinc-900 border-b border-zinc-800 px-10 py-4 flex justify-between items-center sticky top-0">
        <h1 class="text-xl font-semibold tracking-tight">Create User</h1>
        <a href="/" class="px-4 py-2 bg-zinc-800 text-white rounded-lg font-medium hover:bg-zinc-700 transition">
            Back
        </a>
    </nav>

    <!-- FORM CONTAINER -->
    <div class="w-full flex justify-center p-10">

        <form class="w-full max-w-md bg-zinc-900 p-8 rounded-2xl border border-zinc-800 shadow-xl" method="post" action="/create">

            <h2 class="text-2xl font-semibold mb-6 tracking-tight">Add New User</h2>

            <!-- NAME -->
            <div class="mb-4">
                <label class="block mb-1 text-sm text-zinc-400">Name</label>
                <input
                    class="w-full px-4 py-3 rounded-lg bg-zinc-800 border border-zinc-700 focus:border-blue-500 outline-none transition"
                    type="text"
                    name="name"
                    placeholder="Enter name"
                    required>
            </div>

            <!-- EMAIL -->
            <div class="mb-4">
                <label class="block mb-1 text-sm text-zinc-400">Email</label>
                <input
                    class="w-full px-4 py-3 rounded-lg bg-zinc-800 border border-zinc-700 focus:border-blue-500 outline-none transition"
                    type="email"
                    name="email"
                    placeholder="Enter email"
                    required>
            </div>

            <!-- IMAGE -->
            <div class="mb-6">
                <label class="block mb-1 text-sm text-zinc-400">Image URL</label>
                <input
                    class="w-full px-4 py-3 rounded-lg bg-zinc-800 border border-zinc-700 focus:border-blue-500 outline-none transition"
                    type="text"
                    name="image"
                    placeholder="Enter image URL">
            </div>

            <!-- SUBMIT -->
            <input 
                type="submit"
                value="Create User"
                class="w-full py-3 bg-blue-600 text-white font-semibold rounded-lg cursor-pointer hover:bg-blue-500 transition"
            />

        </form>

    </div>

</body>

</html>
