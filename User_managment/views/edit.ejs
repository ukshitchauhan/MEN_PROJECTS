<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 text-white min-h-screen flex items-center justify-center p-6">

    <div class="bg-zinc-800 shadow-lg shadow-black/40 rounded-xl w-80 p-6">

        <h2 class="text-2xl font-semibold text-center mb-4 text-blue-300">Edit User</h2>

        <form action="/edit/<%= users._id %>" method="POST" class="space-y-4">

            <!-- Name -->
            <div>
                <label class="block text-sm font-medium mb-1">Full Name</label>
                <input 
                    type="text" 
                    name="name"
                    class="w-full p-2 rounded-md bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-400"
                    value="<%= users.name %>"
                >
            </div>
            <!-- Email -->
            <div>
                <label class="block text-sm font-medium mb-1">Email</label>
                <input 
                    type="email" 
                    name="email"
                    class="w-full p-2 rounded-md bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-400"
                    value="<%= users.email %>"
                >
            </div>

            <!-- Phone -->
            <div>
                <label class="block text-sm font-medium mb-1">Image</label>
                <input 
                    type="text" 
                    name="image"
                    class="w-full p-2 rounded-md bg-zinc-700 border border-zinc-600 focus:outline-none focus:ring-2 focus:ring-blue-400"
                    value="<%= users.image %>"
                >
            </div>

            <!-- Submit -->
            <input 
                type="submit" 
                value="Update User"
                class="w-full bg-blue-500 hover:bg-blue-600 cursor-pointer font-semibold py-2 rounded-md mt-2"
            >

        </form>

        <a 
            href="/" 
            class="block text-center text-sm mt-4 text-zinc-400 hover:text-zinc-200"
        >
            ← Back to Home
        </a>

    </div>

</body>
</html>
