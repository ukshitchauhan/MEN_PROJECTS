<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stream | MyApp</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
        ::-webkit-scrollbar { width: 0px; background: transparent; } /* Hidden scrollbar for minimalism */
        
        /* Subtle float animation for the background */
        @keyframes float {
            0% { transform: translate(0px, 0px) scale(1); }
            33% { transform: translate(30px, -50px) scale(1.1); }
            66% { transform: translate(-20px, 20px) scale(0.9); }
            100% { transform: translate(0px, 0px) scale(1); }
        }
        .animate-blob { animation: float 10s infinite ease-in-out; }
        .glass-card {
            background: rgba(24, 24, 27, 0.4);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.03);
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
        }
    </style>
</head>

<body class="bg-black text-zinc-100 antialiased selection:bg-indigo-500/30 selection:text-indigo-200 min-h-screen overflow-x-hidden">

    <!-- Background Ambient Glow (Fixed) -->
    <div class="fixed inset-0 z-0 pointer-events-none overflow-hidden">
        <div class="absolute top-0 left-1/4 w-[500px] h-[500px] bg-blue-900/20 rounded-full blur-[120px] animate-blob"></div>
        <div class="absolute bottom-0 right-1/4 w-[500px] h-[500px] bg-violet-900/20 rounded-full blur-[120px] animate-blob" style="animation-delay: 2s"></div>
    </div>

    <div class="relative z-10 flex justify-center">

        <!-- FLOATING SIDEBAR (Desktop) -->
        <aside class="hidden md:flex flex-col items-end fixed left-0 top-0 h-screen w-1/4 p-10 xl:p-16">
            <div class="w-full max-w-[200px] flex flex-col h-full">
                <!-- Brand -->
                <div class="mb-12">
                    <h1 class="text-2xl font-black tracking-tight bg-gradient-to-br from-white to-zinc-500 bg-clip-text text-transparent">MyApp.</h1>
                </div>

                <!-- Nav Links -->
                <nav class="space-y-6 flex-1">
                    <a href="/dashboard" class="flex items-center gap-4 text-white font-medium transition-all group">
                        <div class="w-10 h-10 rounded-full bg-zinc-800/50 flex items-center justify-center group-hover:bg-white group-hover:text-black transition-all">
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M4 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2V6zM14 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2V6zM4 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2v-2zM14 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2v-2z"></path></svg>
                        </div>
                        <span class="text-lg opacity-80 group-hover:opacity-100">Feed</span>
                    </a>
                    
                    <a href="/profile" class="flex items-center gap-4 text-zinc-400 hover:text-white font-medium transition-all group">
                        <div class="w-10 h-10 rounded-full border border-zinc-800 flex items-center justify-center group-hover:border-white transition-all">
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path></svg>
                        </div>
                        <span class="text-lg group-hover:opacity-100">Profile</span>
                    </a>

                    <a href="/profile/create" class="flex items-center gap-4 text-zinc-400 hover:text-white font-medium transition-all group">
                        <div class="w-10 h-10 rounded-full border border-zinc-800 flex items-center justify-center group-hover:border-blue-500 group-hover:text-blue-500 transition-all">
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M12 4v16m8-8H4"/></svg>
                        </div>
                        <span class="text-lg group-hover:opacity-100">Create</span>
                    </a>
                </nav>

                <!-- Logout -->
                <a href="/logout" class="flex items-center gap-4 text-zinc-500 hover:text-red-400 transition-all mt-auto group">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/></svg>
                    <span class="group-hover:translate-x-1 transition-transform">Logout</span>
                </a>
            </div>
        </aside>

        <!-- MAIN FEED STREAM -->
        <main class="w-full md:w-1/2 lg:w-[600px] min-h-screen px-4 pb-20 pt-6 md:pt-16">
            
            <!-- Mobile Header -->
            <div class="md:hidden flex justify-between items-center mb-8 sticky top-0 z-50 py-4 glass-card rounded-2xl px-4">
                <h1 class="font-bold text-xl">MyApp.</h1>
                <div class="flex gap-4">
                    
                    <a href="/profile" class="p-2 bg-zinc-800 rounded-full">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path></svg>
                    </a>
                </div>
            </div>

            <!-- Feed Header -->
            <div class="mb-10 text-center md:text-left px-2">
                <h2 class="text-3xl font-bold text-white mb-2">The Feed</h2>
                <p class="text-zinc-500">See what's happening around the world.</p>
            </div>

            <!-- POSTS (Cards) -->
            <div class="space-y-6">
                <% posts.reverse().forEach(post => { %>

                <!-- Unique Card Design: Glassmorphism + High Border Radius -->
                <div class="glass-card rounded-[2rem] p-6 sm:p-8 hover:bg-zinc-900/60 transition-all duration-500 group relative">
                    
                    <!-- Top Row: User -->
                    <div class="flex items-center justify-between mb-4">
                        <div class="flex items-center gap-3">
                            <div class="w-12 h-12 rounded-2xl bg-gradient-to-br from-zinc-800 to-zinc-700 flex items-center justify-center text-white font-bold shadow-inner">
                                <%= post.user.username.charAt(0).toUpperCase() %>
                            </div>
                            <div>
                                <h3 class="font-bold text-zinc-100 text-lg leading-none"><%= post.user.name %></h3>
                                <p class="text-zinc-500 text-xs mt-1"><%= new Date(post.date).toLocaleString('en-GB', { hour12: false }).replace(',', '') %>
</p>
                            </div>
                        </div>
                    </div>

                    <!-- Content -->
                    <div class="mb-6">
                        <p class="text-zinc-200 text-[1.05rem] leading-relaxed whitespace-pre-wrap font-light tracking-wide"><%= post.content %></p>
                    </div>

                    <!-- Minimal Actions -->
                    <div class="flex items-center gap-6 border-t border-white/5 pt-4">
                        <a href="/like/<%= post._id %>" class="flex items-center gap-2 group/btn transition-colors">
                            <div class="p-2 rounded-full bg-zinc-800/50 group-hover/btn:bg-pink-500/20 transition-colors">
                                <% if(post.likes.length > 0) { %>
                                    <svg class="w-5 h-5 text-pink-500 fill-current" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M3.172 5.172a4 4 0 015.656 0L10 6.343l1.172-1.171a4 4 0 115.656 5.656L10 17.657l-6.828-6.829a4 4 0 010-5.656z" clip-rule="evenodd" /></svg>
                                <% } else { %>
                                    <svg class="w-5 h-5 text-zinc-400 group-hover/btn:text-pink-500 transition-colors" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" /></svg>
                                <% } %>
                            </div>
                            <% if(post.likes.length > 0) { %>
                                <span class="text-sm font-medium text-pink-500"><%= post.likes.length %></span>
                            <% } %>
                        </a>
                        
                        <!-- Share (Visual) -->
                        <button class="ml-auto text-zinc-500 hover:text-white transition-colors">
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M8.684 13.342C8.886 12.938 9 12.482 9 12c0-.482-.114-.938-.316-1.342m0 2.684a3 3 0 110-2.684m0 2.684l6.632 3.316m-6.632-6l6.632-3.316m0 0a3 3 0 105.367-2.684 3 3 0 00-5.367 2.684zm0 9.316a3 3 0 105.368 2.684 3 3 0 00-5.368-2.684z"></path></svg>
                        </button>
                    </div>

                </div>

                <% }) %>
            </div>
            
            <!-- Empty State -->
            <% if(posts.length === 0) { %>
                <div class="text-center py-20 opacity-50">
                    <p class="text-xl font-light">It's quiet here...</p>
                </div>
            <% } %>

            <div class="h-20"></div> <!-- Bottom Spacer -->
        </main>

    </div>

</body>
</html>