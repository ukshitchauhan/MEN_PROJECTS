<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | MyApp</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
        @keyframes float {
            0% { transform: translate(0px, 0px) scale(1); }
            33% { transform: translate(30px, -50px) scale(1.1); }
            66% { transform: translate(-20px, 20px) scale(0.9); }
            100% { transform: translate(0px, 0px) scale(1); }
        }
        .animate-blob { animation: float 10s infinite ease-in-out; }
        .glass-card {
            background: rgba(24, 24, 27, 0.6);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.05);
            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.3);
        }
        input:-webkit-autofill {
            -webkit-box-shadow: 0 0 0 30px #09090b inset !important;
            -webkit-text-fill-color: white !important;
        }
    </style>
</head>

<body class="bg-black text-white flex items-center justify-center min-h-screen relative overflow-hidden selection:bg-indigo-500/30">

    <!-- Ambient Glow -->
    <div class="fixed inset-0 z-0 pointer-events-none">
        <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] bg-violet-900/20 rounded-full blur-[120px] animate-blob"></div>
    </div>

    <div class="w-full max-w-md glass-card p-10 rounded-[2.5rem] relative z-10 mx-4">
        
        <div class="mb-10 text-center">
            <h1 class="text-3xl font-black tracking-tight mb-2 bg-gradient-to-r from-white to-zinc-400 bg-clip-text text-transparent">
                Welcome back.
            </h1>
            <p class="text-zinc-500 text-sm">Enter your credentials.</p>
        </div>

        <form action="/login" method="post" class="space-y-5">
            <div class="space-y-4">
                <input class="w-full px-6 py-4 rounded-2xl bg-white/5 border border-white/5 placeholder-zinc-600 text-zinc-200 focus:bg-white/10 focus:border-white/20 focus:outline-none transition-all" 
                       type="text" name="email" placeholder="Email">

                <input class="w-full px-6 py-4 rounded-2xl bg-white/5 border border-white/5 placeholder-zinc-600 text-zinc-200 focus:bg-white/10 focus:border-white/20 focus:outline-none transition-all"
                       type="password" name="password" placeholder="Password">
            </div>

            <button type="submit"
                class="w-full mt-6 bg-white text-black hover:bg-zinc-200 py-4 rounded-2xl font-bold tracking-wide transition-all active:scale-[0.98]">
                Sign In
            </button>
        </form>

        <div class="mt-8 text-center">
            <a href="/" class="text-zinc-500 hover:text-white text-sm transition-colors">Create an account</a>
        </div>
    </div>

</body>
</html>