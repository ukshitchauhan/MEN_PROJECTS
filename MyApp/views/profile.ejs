<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile | MyApp</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
        ::-webkit-scrollbar { width: 0px; background: transparent; }
        
        @keyframes float {
            0% { transform: translate(0px, 0px) scale(1); }
            33% { transform: translate(30px, -50px) scale(1.1); }
            66% { transform: translate(-20px, 20px) scale(0.9); }
            100% { transform: translate(0px, 0px) scale(1); }
        }
        .animate-blob { animation: float 10s infinite ease-in-out; }
        .glass-card {
            background: rgba(24, 24, 27, 0.4);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.03);
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
        }
    </style>
</head>

<body class="bg-black text-zinc-100 antialiased selection:bg-indigo-500/30 selection:text-indigo-200 min-h-screen">

    <!-- Ambient Glow -->
    <div class="fixed inset-0 z-0 pointer-events-none overflow-hidden">
        <div class="absolute top-0 right-1/4 w-[500px] h-[500px] bg-blue-900/20 rounded-full blur-[120px] animate-blob"></div>
        <div class="absolute bottom-0 left-1/4 w-[500px] h-[500px] bg-indigo-900/20 rounded-full blur-[120px] animate-blob" style="animation-delay: 2s"></div>
    </div>

    <div class="relative z-10 flex justify-center">

        <!-- FLOATING SIDEBAR (Consistent with Dashboard) -->
        <aside class="hidden md:flex flex-col items-end fixed left-0 top-0 h-screen w-1/4 p-10 xl:p-16">
            <div class="w-full max-w-[200px] flex flex-col h-full">
                <div class="mb-12">
                    <h1 class="text-2xl font-black tracking-tight bg-gradient-to-br from-white to-zinc-500 bg-clip-text text-transparent">MyApp.</h1>
                </div>
                <nav class="space-y-6 flex-1">
                    <a href="/dashboard" class="flex items-center gap-4 text-zinc-400 hover:text-white font-medium transition-all group">
                        <div class="w-10 h-10 rounded-full border border-zinc-800 flex items-center justify-center group-hover:border-white transition-all">
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M4 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2V6zM14 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2V6zM4 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2v-2zM14 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2v-2z"></path></svg>
                        </div>
                        <span class="text-lg group-hover:opacity-100">Feed</span>
                    </a>
                    <a href="/profile" class="flex items-center gap-4 text-white font-medium transition-all group">
                        <div class="w-10 h-10 rounded-full bg-zinc-800/50 flex items-center justify-center group-hover:bg-white group-hover:text-black transition-all">
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path></svg>
                        </div>
                        <span class="text-lg opacity-80 group-hover:opacity-100">Profile</span>
                    </a>
                    <a href="/logout" class="flex items-center gap-4 text-zinc-500 hover:text-red-400 transition-all mt-auto group">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/></svg>
                        <span class="group-hover:translate-x-1 transition-transform">Logout</span>
                    </a>
                </nav>
            </div>
        </aside>

        <!-- MAIN CONTENT -->
        <main class="w-full md:w-1/2 lg:w-[600px] min-h-screen px-4 pb-20 pt-6 md:pt-16">

            <!-- Mobile Header -->
            <div class="md:hidden flex justify-between items-center mb-8 sticky top-0 z-50 py-4 glass-card rounded-2xl px-4">
                <a href="/dashboard" class="text-zinc-400"><svg class="w-6 h-6" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M10 19l-7-7m0 0l7-7m-7 7h18"/></svg></a>
                <h1 class="font-bold text-lg"><%= user.username %></h1>
                <div class="w-6"></div> <!-- Spacer -->
            </div>

            <!-- Profile Info Card -->
            <div class="glass-card rounded-[2.5rem] p-8 mb-6 relative overflow-hidden">
                <div class="absolute top-0 right-0 w-32 h-32 bg-white/5 rounded-full blur-2xl -mr-10 -mt-10"></div>
                
                <div class="flex flex-col items-center justify-center text-center relative z-10">
                    <div class="w-24 h-24 rounded-3xl bg-gradient-to-tr from-white to-zinc-400 p-[2px] mb-4">
                        <div class="w-full h-full bg-black rounded-[22px] flex items-center justify-center text-3xl font-bold text-white">
                            <%= user.username.charAt(0).toUpperCase() %>
                        </div>
                    </div>
                    
                    <h1 class="text-3xl font-bold text-white mb-1"><%= user.username %></h1>
                    <div class="inline-flex items-center px-4 py-1.5 rounded-full bg-white/5 border border-white/5 text-sm text-zinc-400 mb-6">
                        <span class="text-white font-bold mr-1.5"><%= user.posts.length %></span> Posts
                    </div>
                </div>
            </div>

            <!-- Create Post (Collapsed Style) -->
            <div class="glass-card rounded-[2rem] p-2 mb-8">
                <form action="/post" method="post" class="relative">
                    <textarea 
                        name="content" 
                        placeholder="Share your thoughts..." 
                        class="w-full bg-transparent text-white placeholder-zinc-500 p-4 focus:outline-none resize-none min-h-[60px] text-lg rounded-[1.5rem]"
                    ></textarea>
                    <div class="flex justify-end px-2 pb-2">
                        <button type="submit" class="bg-white text-black px-6 py-2 rounded-full font-bold hover:bg-zinc-200 transition-colors">
                            Post
                        </button>
                    </div>
                </form>
            </div>

            <!-- Posts List -->
            <div class="space-y-6">
                <% if(user.posts.length === 0) { %>
                    <div class="text-center py-20 opacity-50">
                        <p class="text-lg font-light">No posts yet.</p>
                    </div>
                <% } else { %>
                    <% user.posts.reverse().forEach(post => { %>
                        <div class="glass-card rounded-[2rem] p-6 sm:p-8 hover:bg-zinc-900/60 transition-all duration-500 group relative">
                            
                            <div class="flex justify-between items-start mb-4">
                                <div class="flex items-center gap-3">
                                    <div class="w-10 h-10 rounded-xl bg-zinc-800 flex items-center justify-center text-white font-bold text-sm">
                                        You
                                    </div>
                                    <span class="text-zinc-500 text-xs">Just now</span>
                                </div>
                                <a href="/edit/<%= post._id %>" class="p-2 rounded-full hover:bg-white/10 text-zinc-500 hover:text-white transition-colors">
                                    <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z"/></svg>
                                </a>
                            </div>

                            <p class="text-zinc-200 text-[1.05rem] leading-relaxed whitespace-pre-wrap font-light"><%= post.content %></p>

                            <div class="mt-6 flex items-center gap-2 text-sm text-pink-500">
                                <svg class="w-4 h-4 fill-current" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M3.172 5.172a4 4 0 015.656 0L10 6.343l1.172-1.171a4 4 0 115.656 5.656L10 17.657l-6.828-6.829a4 4 0 010-5.656z" clip-rule="evenodd" /></svg>
                                <span><%= post.likes.length %></span>
                            </div>
                        </div>
                    <% }) %>
                <% } %>
            </div>

            <div class="h-20"></div>
        </main>

    </div>

</body>
</html>